<section id="demos">
      <div class="row">
        <div class="large-12 columns">
          <div class="owl-carousel owl-theme">
              {{#each list}}
                <div class="item">
                 <img src={{img}} />
                 <div class="productTitle">{{title}}</div>
                 <div class="productPrize">{{price}}</div>
                 <div class="viewItem">View Item</div>
                 <!--div class="btnGroup">
                    <div class="viewBtn">View</div>
                    <div class="buyBtn">Buy</div>
                  </div-->
                </div>
              {{/each}}
          </div>
        </div>
      </div>
</section>